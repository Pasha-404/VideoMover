<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_app_background"
    android:fitsSystemWindows="true">

    <!-- Отдельный Toolbar с ЯВНОЙ иконкой "назад" и её тинтом -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/vm_bg"
        android:elevation="4dp"
        app:title="@string/settings"
        app:titleTextColor="@color/vm_text"
        app:titleCentered="false"
        app:navigationIcon="@drawable/ic_arrow_back_24"
        app:navigationIconTint="@color/vm_text" />

    <!-- Сохраняем ваш контейнер с тем же id -->
    <FrameLayout
        android:id="@+id/settings"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="?attr/actionBarSize" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
